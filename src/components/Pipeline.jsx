import Icon from './Icon.jsx'
import { PageHeading } from './Ui.jsx'
import { stages } from '../data.js'

export default function Pipeline({ onNewLead }) {
  return <><PageHeading title="Pipeline" description="Every conversation, deal, and next move in one view." action="New deal" onAction={onNewLead}/><div className="pipeline-toolbar"><label className="search-box"><Icon name="search" size={17}/><input placeholder="Search deals or companies" /></label><button className="filter-button">All owners <span>⌄</span></button><button className="filter-button">This quarter <span>⌄</span></button><span className="pipeline-value">Open pipeline <strong>ETB 3.84M</strong></span></div><section className="pipeline-board">{stages.map(stage => <div className="pipeline-column" key={stage.key}><div className="column-heading"><span className={`stage-icon ${stage.accent}`}><Icon name={stage.icon} size={16}/></span><strong>{stage.key}</strong><b>{stage.count}</b></div><div className="column-amount">{stage.amount}</div><div className="deal-stack">{stage.deals.map(([company, type, value], index) => <article className="deal-card" key={company}><div><span className={`status-dot ${index % 3 === 1 ? 'warm' : index % 3 === 2 ? 'cold' : ''}`}/><small>{index === 0 ? 'Follow-up today' : type}</small><button aria-label={`More actions for ${company}`}><Icon name="dots" size={15}/></button></div><h3>{company}</h3><p>{type}</p><footer><span>{index % 2 === 0 ? 'Haron' : 'Sara'}</span><strong>{value}</strong></footer></article>)}</div><button className="add-deal-column"><Icon name="plus" size={15}/>Add deal</button></div>)}</section></>
}
